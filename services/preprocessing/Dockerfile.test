# Dockerfile pour les tests
# Utiliser python:3.11 si python:3.11-slim n'est pas accessible (problème de proxy)
# Alternative : FROM python:3.11-alpine (plus petite, plus rapide)
FROM python:3.11

# Métadonnées
LABEL maintainer="predictive-maintenance-team"
LABEL description="Service Prétraitement - Tests"

# Variables d'environnement
ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

# Répertoire de travail
WORKDIR /app

# Installation des dépendances système
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    g++ \
    libpq-dev \
    netcat-openbsd \
    && rm -rf /var/lib/apt/lists/*

# Copier requirements.txt
COPY requirements.txt .

# Installer les dépendances Python
RUN pip install --no-cache-dir -r requirements.txt

# Copier le code source
COPY . .

# Commande par défaut (peut être surchargée)
CMD ["pytest", "tests/", "-v", "--tb=short"]

