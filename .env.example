# PostgreSQL / TimescaleDB Configuration
POSTGRES_DB=predictive_maintenance
POSTGRES_USER=pmuser
POSTGRES_PASSWORD=pmpassword

# InfluxDB Configuration
INFLUXDB_USER=admin
INFLUXDB_PASSWORD=adminpassword
INFLUXDB_ORG=pm-org
INFLUXDB_BUCKET=sensor-data
INFLUXDB_TOKEN=pm-token-change-in-production

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Redis Configuration
REDIS_PASSWORD=redispassword

# pgAdmin Configuration (optional)
PGADMIN_EMAIL=admin@pm.local
PGADMIN_PASSWORD=admin

# Kafka Topics (will be created by scripts)
# Topics: sensor-data, preprocessed-data, features, anomalies, rul-predictions, maintenance-orders

# Application Configuration
# These will be used by services in future phases
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
DATABASE_URL=jdbc:postgresql://postgresql:5432/predictive_maintenance
TIMESCALEDB_URL=jdbc:postgresql://postgresql:5432/predictive_maintenance
INFLUXDB_URL=http://influxdb:8086
MINIO_ENDPOINT=http://minio:9000
REDIS_HOST=redis
REDIS_PORT=6379

# Environment
ENVIRONMENT=development
