apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: predictive-maintenance
  labels:
    app: kafka
spec:
  type: ClusterIP
  ports:
  - port: 9092
    targetPort: 9092
    protocol: TCP
    name: kafka
  selector:
    app: kafka

